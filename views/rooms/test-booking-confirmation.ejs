<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .success { color: green; }
        .error { color: red; }
    </style>
</head>
<body>
    <h1>Booking Confirmation Test</h1>
    
    <div class="success">
        <h2>✅ Template is working!</h2>
        <p>Room: <%= room.type %> (<%= room.roomNumber %>)</p>
        <p>Price: ₦<%= room.price.toLocaleString() %></p>
        <p>Check-in: <%= checkIn %></p>
        <p>Check-out: <%= checkOut %></p>
        <p>Guests: <%= guests %></p>
        <p>Nights: <%= nights %></p>
        <p>Total: ₦<%= totalAmount.toLocaleString() %></p>
    </div>

    <hr>
    
    <h3>Debug Info:</h3>
    <pre><%= JSON.stringify({ room: room, checkIn, checkOut, guests, nights, totalAmount }, null, 2) %></pre>

    <a href="/rooms">Back to Rooms</a>
</body>
</html>